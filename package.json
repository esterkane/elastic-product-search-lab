{
  "name": "elastic-search-policy-control-plane",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Governed Elasticsearch search companion with indexed policy lookup, strategy routing, conflict resolution, explain traces, and promotion workflow.",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "node --test tests/*.test.ts",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json",
    "evaluate:relevance": "node scripts/run_search_quality.mjs"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  }
}
