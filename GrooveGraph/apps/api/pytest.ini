[pytest]
markers =
    unit: fast tests with mocked dependencies or in-memory stores
    integration: tests that require Dockerized backing services
    e2e: end-to-end smoke tests across service boundaries
    requires_docker: tests that require docker compose services to be running
